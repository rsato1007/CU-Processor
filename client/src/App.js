import React from 'react';

// Adding this as a reminder to not forget to add
// validators to client side when doing things like
// adding memebr number, new member, etc.

// Another thing, I found a possible API for address.
// Use this when doing address: https://www.smarty.com/articles/usps-api
// This might be the same one that prodigy uses.

export default function App() {
    return (
        <div>Hello World</div>
    )
};